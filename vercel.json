{
  "buildCommand": "cd backoffice && npm install && npm run build",
  "outputDirectory": "backoffice/.next",
  "installCommand": "cd backoffice && npm install",
  "framework": "nextjs",
  "rewrites": [
    { "source": "/(.*)", "destination": "/$1" }
  ]
}
